import sys

# Interating through each line from the standard SInput.
for InputData in sys.stdin:
    InputData = InputData.strip()
    print(InputData)
    print("----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------")
